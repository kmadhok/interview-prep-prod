.DS_Store
.obsidian/workspace*
.obsidian/cache
*.html
__pycache__/
*.pyc

# Claude Code local machine settings
.claude/settings.local.json
.apollo-scrape/

# Secrets — API keys (EmailFinder, LeadMagic, etc.)
.env

# verify-emails derived credit cache (local, regenerated on demand)
.claude/skills/verify-emails/.email-cache.json

# Trace output — one dir per run, instance data, never committed
runs/

# codex-cli AGENTS.md compatibility mirror — tool artifact, never repo content
.agents/
